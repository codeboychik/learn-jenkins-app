pipeline {
    agent any

    stages {
        stage('Docker build') {
            steps {
                sh '''
                    docker build -f Dockerfile.pw -t playwright .
                   '''
            }
        }
    }
}