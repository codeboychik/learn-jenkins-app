pipeline {
    agent {
        docker {
            image 'oven/bun:latest'
            args '-u 0'
        }
    }
    stages {
        stage('Install Dependencies') {
            steps {
                sh '''
                    bun install
                    bun -v
                    bunx playwright install
                   '''
            }
        }
        stage('Build and Test') {
            steps {
                sh ''' 
                    npm install -g bun
                    bun run testp
                    bun run jest
                    bun run build
                   '''
            }
        }
    }
}