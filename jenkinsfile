pipeline {
    agent {
        docker {
            image 'node:22-alpine'
            reuseNode true
        }
    }
    stages {
        stage('Install Dependencies') {
            steps {
                sh '''
                    npm install bun
                    bun install
                    bun -v
                    bunx playwright install
                   '''
            }
        }
        stage('Build and Test') {
            steps {
                sh ''' 
                    bun run testp
                    bun run jest
                    bun run build
                   '''
            }
        }
    }
}