pipeline {
    agent {
        docker {
            image 'oven/bun:debian'
            reuseNode true
        }
    }
    stages {
        stage('Install Dependencies') {
            steps {
                sh '''
                    ls -la
                    bun install
                    bun -v
                    bunx playwright install
                   '''
            }
        }
        stage('Build and Test') {
            steps {
                sh ''' 
                    bun run testp
                    bun run jest
                    bun run build
                   '''
            }
        }
    }
    post {
        always {
            cleanWs()
        }
    }
}